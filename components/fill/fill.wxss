/* components/fill/fill.wxss */
@import "../../colorui/main.wxss";
@import "../../colorui/icon.wxss";
.fill{
  padding: 0;
	height: calc(100rpx + env(safe-area-inset-bottom) / 2);
	padding-bottom: calc(env(safe-area-inset-bottom) / 2);
}